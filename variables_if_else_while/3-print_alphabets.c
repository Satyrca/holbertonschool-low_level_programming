#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints a text according number
 * Return: Always (Success)
 *
 */

int main(void)

{

	char low;

	for (low = 'a'; low <= 'a'; low++)
	putchar(low);

	for (low = 'A'; low <= 'B'; low++)
	putchar(low);
	putchar('\n');

        return (0);

}
